
class ConexionCloaca {

    barrio;
    tipo;
    precio;
    derecho;
    decimal;
  
    constructor(barrio, tipo, precio, derecho, decimal) {
      this.barrio = barrio;
      this.tipo = tipo;
      this.precio = precio;
      this.derecho = derecho;
      this.decimal = decimal;
    }
  }